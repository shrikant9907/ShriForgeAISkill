# Example: "Add an admissions page"

Expected routing in an existing site:

- `FEATURE`
- repository discovery only to the depth needed to understand current patterns
- feature workflow
- truth + IA/UX/content/frontend
- framework adapter
- SEO/accessibility/testing
- broader local search/structured data only if the page contains relevant verified data

Do not run a complete redesign or migration.
