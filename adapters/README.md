# Framework Adapters

Adapters contain framework-specific implementation guidance. Core ShriForgeAISkill rules remain framework-neutral.

Load exactly one adapter when implementation depends on it. If the repository uses an unsupported framework, follow its established conventions and current authoritative documentation instead of forcing an adapter.
